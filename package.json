{
  "name": "imporsan-prospect",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "uv sync --project backend && cd frontend && npm install",
    "dev": "concurrently -n backend,frontend -c cyan.bold,magenta.bold --kill-others-on-fail \"cd backend && .venv/bin/python3 run.py\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && .venv/bin/python3 run.py",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
